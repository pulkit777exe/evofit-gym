import Marquee from "react-fast-marquee";

export default function MarqueeElement(){
    return (
        <Marquee className="bg-gym-dark text-gym-light text-2xl md:text-4xl p-4 font-bold">
            “ It doesn’t get easier. You get stronger with every session. ”
        </Marquee>
    )
}